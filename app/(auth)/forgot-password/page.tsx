'use client'; import {useState} from 'react'; import {Button} from '@/components/ui/button'; import {Card} from '@/components/ui/card'; import {Alert} from '@/components/ui/alert';
export default function ForgotPassword(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const j=await r.json();setMsg(r.ok?j.data.message:j.message)};return <main className="grid min-h-[calc(100vh-73px)] place-items-center px-4 py-12"><Card className="w-full max-w-md p-7"><h1 className="text-3xl font-black">Reset your password</h1><p className="mt-2 text-sm text-[var(--muted)]">Enter your email. The production version should deliver a one-time reset link through a verified email provider.</p>{msg&&<div className="mt-5"><Alert tone="info">{msg}</Alert></div>}<form onSubmit={submit} className="mt-6"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="w-full rounded-xl border border-[var(--border)] bg-transparent p-3"/><Button type="submit" className="mt-3 w-full">Request reset</Button></form></Card></main>}
