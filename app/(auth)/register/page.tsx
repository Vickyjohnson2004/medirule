'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {useForm} from 'react-hook-form'; import {zodResolver} from '@hookform/resolvers/zod'; import {registerSchema} from '@/validators/auth'; import type {z} from 'zod'; import {Button} from '@/components/ui/button'; import {Card} from '@/components/ui/card'; import {Alert} from '@/components/ui/alert'; import Link from 'next/link';
type Form=z.infer<typeof registerSchema>;
export default function Register(){const router=useRouter();const [error,setError]=useState('');const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm<Form>({resolver:zodResolver(registerSchema)});const submit=async(data:Form)=>{setError('');const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok){setError(j.message||'Unable to create account');return}router.push('/dashboard')};return <main className="grid min-h-[calc(100vh-73px)] place-items-center px-4 py-12"><Card className="w-full max-w-md p-7"><p className="text-sm font-semibold text-[var(--primary)]">Create your account</p><h1 className="mt-2 text-3xl font-black">Start with MediRule</h1>{error&&<div className="mt-5"><Alert tone="danger">{error}</Alert></div>}<form onSubmit={handleSubmit(submit)} className="mt-6 space-y-4"><label className="block text-sm font-semibold">Full name<input {...register('name')} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-transparent px-3 py-3"/>{errors.name&&<span className="text-xs text-red-600">Use at least 2 characters.</span>}</label><label className="block text-sm font-semibold">Email<input {...register('email')} type="email" autoComplete="email" className="mt-2 w-full rounded-xl border border-[var(--border)] bg-transparent px-3 py-3"/>{errors.email&&<span className="text-xs text-red-600">Enter a valid email.</span>}</label><label className="block text-sm font-semibold">Password<input {...register('password')} type="password" autoComplete="new-password" className="mt-2 w-full rounded-xl border border-[var(--border)] bg-transparent px-3 py-3"/>{errors.password&&<span className="text-xs text-red-600">Use at least 8 characters.</span>}</label><Button type="submit" disabled={isSubmitting} className="w-full">{isSubmitting?'Creating…':'Create account'}</Button></form><p className="mt-6 text-center text-sm text-[var(--muted)]">Already registered? <Link className="font-semibold text-[var(--primary)]" href="/login">Sign in</Link></p></Card></main>}
