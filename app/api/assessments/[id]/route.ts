import {NextRequest} from 'next/server'; import Assessment from '@/models/Assessment'; import AssessmentResult from '@/models/AssessmentResult'; import {connectDB} from '@/lib/db'; import {requireUser} from '@/lib/auth'; import {apiError,apiSuccess} from '@/lib/security';
export async function GET(_req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(['patient','professional','admin']);const {id}=await params;await connectDB();const assessment=await Assessment.findById(id).populate('symptoms.symptomId','name category').lean();if(!assessment)return apiError('Assessment not found','NOT_FOUND',404);if(user.role==='patient'&&String(assessment.userId)!==String(user._id))return apiError('You cannot access this assessment','FORBIDDEN',403);const result=await AssessmentResult.findOne({assessmentId:id}).lean();return apiSuccess({assessment,result});}catch(e:any){if(e.message==='UNAUTHORIZED')return apiError('Please sign in','UNAUTHORIZED',401);return apiError('Unable to load assessment','ASSESSMENT_FETCH_ERROR',500);}}
export async function DELETE(_req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(['patient','admin']);const {id}=await params;await connectDB();const assessment=await Assessment.findById(id);if(!assessment)return apiError('Assessment not found','NOT_FOUND',404);if(user.role==='patient'&&String(assessment.userId)!==String(user._id))return apiError('Forbidden','FORBIDDEN',403);await AssessmentResult.deleteOne({assessmentId:id});await Assessment.deleteOne({_id:id});return apiSuccess({deleted:true});}catch(e:any){if(e.message==='UNAUTHORIZED')return apiError('Please sign in','UNAUTHORIZED',401);return apiError('Unable to delete assessment','DELETE_ERROR',500);}}
