import {NextRequest} from 'next/server'; import Symptom from '@/models/Symptom'; import {connectDB} from '@/lib/db'; import {apiError,apiSuccess} from '@/lib/security';
export async function GET(req:NextRequest){try{await connectDB();const q=req.nextUrl.searchParams.get('q')?.trim()||'';const category=req.nextUrl.searchParams.get('category');const filter:any={isActive:true};if(category)filter.category=category;if(q)filter.$text={$search:q};const symptoms=await Symptom.find(filter).sort(q?{score:{$meta:'textScore'} as any}:{category:1,name:1}).limit(100).lean();return apiSuccess(symptoms);}catch(e){console.error(e);return apiError('Unable to load symptoms','SYMPTOM_FETCH_ERROR',500);}}
