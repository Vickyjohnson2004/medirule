import {Card} from '@/components/ui/card'; import {BrainCircuit,ShieldAlert,Scale,Database} from 'lucide-react';
export default function HowItWorks(){return <main className="mx-auto max-w-6xl px-4 py-12 sm:px-6"><p className="text-sm font-bold uppercase tracking-wider text-[var(--primary)]">System architecture</p><h1 className="mt-2 text-4xl font-black">Explainable inference, separated by responsibility.</h1><p className="mt-4 max-w-3xl text-[var(--muted)]">The system does not attempt to diagnose. It transforms user input into structured symptom concepts, evaluates safety rules, matches versioned knowledge-base rules, assigns an application risk level, and produces a general advisory.</p><div className="mt-10 grid gap-3">{[['User interface','Collects symptoms, details and follow-up answers.'],['API layer','Validates requests, authenticates users and enforces authorization.'],['Symptom resolver','Maps known symptom IDs and stores unrecognized text for review.'],['Red-flag engine','Checks emergency warning rules before normal interpretation.'],['Rule engine','Calculates weighted rule matches against active, versioned rules.'],['Risk engine','Combines safety signals and rule risk levels into LOW, MODERATE, HIGH or EMERGENCY.'],['Advisory engine','Returns general, non-prescriptive health guidance.'],['MongoDB','Stores users, knowledge base, assessments, result snapshots and audit records.']].map(([title,desc],i)=><div key={title} className="grid gap-3 md:grid-cols-[70px_1fr] md:items-center"><div className="grid size-12 place-items-center rounded-2xl bg-[var(--accent)] font-black text-[var(--primary)]">{i+1}</div><Card className="p-5"><h2 className="font-bold">{title}</h2><p className="mt-1 text-sm text-[var(--muted)]">{desc}</p></Card></div>)}</div><div className="mt-10 grid gap-4 md:grid-cols-3"><Card className="p-5"><BrainCircuit className="text-[var(--primary)]"/><h3 className="mt-4 font-bold">Rule matching</h3><p className="mt-2 text-sm text-[var(--muted)]">Weighted symptoms produce a rule match score only. Scores are never medical probabilities.</p></Card><Card className="p-5"><ShieldAlert className="text-[var(--primary)]"/><h3 className="mt-4 font-bold">Safety first</h3><p className="mt-2 text-sm text-[var(--muted)]">Red-flag signals have higher priority and can produce an emergency risk level.</p></Card><Card className="p-5"><Scale className="text-[var(--primary)]"/><h3 className="mt-4 font-bold">Traceability</h3><p className="mt-2 text-sm text-[var(--muted)]">Assessment results preserve the rule version used at analysis time.</p></Card></div></main>}
