'use client'; import {useState} from 'react'; import {Card} from '@/components/ui/card'; import {Button} from '@/components/ui/button'; import {Alert} from '@/components/ui/alert';
export default function Settings(){const [msg,setMsg]=useState('');const [password,setPassword]=useState({currentPassword:'',newPassword:''});const change=async()=>{const r=await fetch('/api/account',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(password)});const j=await r.json();setMsg(r.ok?'Password changed successfully.':j.message||'Unable to change password.');};const remove=async()=>{if(!confirm('Permanently delete your account and assessment history?'))return;const r=await fetch('/api/account',{method:'DELETE'});if(r.ok)location.href='/';};return <main className="mx-auto max-w-3xl px-4 py-10 sm:px-6"><h1 className="text-3xl font-black">Account settings</h1>{msg&&<div className="mt-5"><Alert tone="info">{msg}</Alert></div>}<Card className="mt-7 p-6"><h2 className="font-bold">Change password</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><input type="password" placeholder="Current password" value={password.currentPassword} onChange={e=>setPassword({...password,currentPassword:e.target.value})} className="rounded-xl border border-[var(--border)] bg-transparent p-3"/><input type="password" placeholder="New password (8+ characters)" value={password.newPassword} onChange={e=>setPassword({...password,newPassword:e.target.value})} className="rounded-xl border border-[var(--border)] bg-transparent p-3"/></div><Button className="mt-4" onClick={change}>Change password</Button></Card><Card className="mt-5 border-red-200 p-6"><h2 className="font-bold text-red-700">Delete account</h2><p className="mt-2 text-sm text-[var(--muted)]">This removes your account, health profile, assessment history and unknown symptom submissions associated with your account.</p><Button className="mt-4" variant="danger" onClick={remove}>Delete account</Button></Card></main>}
