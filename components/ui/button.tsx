import Link from 'next/link';
export function Button({children,href,type='button',variant='primary',className='',disabled=false,onClick}:{children:React.ReactNode;href?:string;type?:'button'|'submit';variant?:'primary'|'secondary'|'ghost'|'danger';className?:string;disabled?:boolean;onClick?:()=>void|Promise<void>}){const c=`inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition focus:outline-none focus:ring-2 focus:ring-[var(--primary)] disabled:cursor-not-allowed disabled:opacity-50 ${variant==='primary'?'bg-[var(--primary)] text-white hover:bg-[var(--primary-strong)]':variant==='secondary'?'border border-[var(--border)] bg-[var(--card)] hover:bg-[var(--accent)]':variant==='danger'?'bg-red-600 text-white hover:bg-red-700':'text-[var(--muted)] hover:bg-[var(--accent)]'} ${className}`; return href?<Link className={c} href={href}>{children}</Link>:<button className={c} type={type} disabled={disabled} onClick={onClick}>{children}</button>}
