import mongoose,{Schema,Document,Model,Types} from 'mongoose';
export interface IUnknownSymptom extends Document {text:string;normalizedText:string;userId:Types.ObjectId;assessmentId:Types.ObjectId;status:'pending'|'reviewed'|'accepted'|'rejected';occurrenceCount:number;reviewedBy?:Types.ObjectId;reviewedAt?:Date;createdAt:Date;updatedAt:Date}
const schema=new Schema<IUnknownSymptom>({text:{type:String,required:true,maxLength:300},normalizedText:{type:String,required:true,index:true},userId:{type:Schema.Types.ObjectId,ref:'User',required:true},assessmentId:{type:Schema.Types.ObjectId,ref:'Assessment',required:true},status:{type:String,enum:['pending','reviewed','accepted','rejected'],default:'pending',index:true},occurrenceCount:{type:Number,default:1},reviewedBy:{type:Schema.Types.ObjectId,ref:'User'},reviewedAt:Date},{timestamps:true}); schema.index({normalizedText:1,status:1}); export default (mongoose.models.UnknownSymptom as Model<IUnknownSymptom>)||mongoose.model<IUnknownSymptom>('UnknownSymptom',schema);
